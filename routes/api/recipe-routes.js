const router = require('express').Router();
const { Recipe } = require('../../models');

//todo: add recipe to account

router.get('/', (req, res) => {

    Recipe.create({

    })


})

module.exports = router;